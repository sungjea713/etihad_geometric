PORT=5016
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
CESIUM_ION_TOKEN=

# Optional: set when deploying on a host whose IPs OpenSky blocks
# (e.g. Render). Point at a Cloudflare Worker proxy — see
# cloudflare-worker/README.md.
# OPENSKY_AUTH_PROXY_URL=https://your-worker.workers.dev/opensky-auth
# OPENSKY_PROXY_URL=https://your-worker.workers.dev/opensky
